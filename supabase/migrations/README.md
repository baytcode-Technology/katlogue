# Migrations moved

Canonical Supabase migrations are at the repository root:

**[`../../../supabase/migrations/`](../../../supabase/migrations/)**

Supabase GitHub integration requires `supabase/config.toml` and `supabase/migrations/` at the repo root (working directory `.`).

Do not add new migration files here — add them under `supabase/migrations/` instead.
